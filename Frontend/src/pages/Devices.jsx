import PlaceholderPanel from "../components/PlaceholderPanel";

export default function Devices() {
  return (
    <div className="devices-page">
      <PlaceholderPanel title="Devices" note="This screen isn't built yet — coming soon." />
    </div>
  );
}
